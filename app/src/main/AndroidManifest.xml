<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.gotev.sipservice">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_SIP" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.audio.low_latency"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="net.gotev.sipservice.client.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SipService"
            android:enabled="true"
            android:exported="true" />

        <receiver android:name="net.gotev.sipservice.client.CallReceiver"  android:exported="true">
            <intent-filter>
                <action android:name="net.gotev.REGISTRATION"/>
                <action android:name="net.gotev.INCOMING_CALL"/>
                <action android:name="net.gotev.CALL_STATE"/>
                <action android:name="net.gotev.OUTGOING_CALL"/>
                <action android:name="net.gotev.STACK_STATUS"/>
                <action android:name="net.gotev.CODEC_PRIORITIES"/>
                <action android:name="net.gotev.CODEC_PRIORITIES_SET_STATUS"/>
                <action android:name="net.gotev.MISSED_CALL"/>
                <action android:name="net.gotev.VIDEO_SIZE"/>
                <action android:name="net.gotev.CALL_STATS"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>